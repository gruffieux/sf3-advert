{# Cette vue n'hérite de personne. Elle est indépendante. #}

<ul class="nav nav-pills nav-stacked">
	{% for advert in listAdverts %}
		<li>
			<a href="{{ path('gbrfix_platform_view', {'advert_id':advert.id}) }}">
				{{ advert.title }}
			</a>
		</li>
	{% endfor %}
</ul>
